# kickoff: none

Nothing is ready: T12 (the first external project migration) is `done 96e0322` and the only other node, `publish`, is `blocked: human call` — pushing 0.1.0 to `Popschlock/graph-engineering` is the owner's word and never automatic (`ge/plugin/calls.md` `## Open`). When he says push, `ge.py set plugin publish --status open` and this file becomes that node's kickoff.
