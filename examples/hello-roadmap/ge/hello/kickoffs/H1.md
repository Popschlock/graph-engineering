# kickoff: H1

Read `hello.py` and `test_hello.py`; `greet` exists. Run `python -m pytest -q` (1 passed), then close H1 as the rules say: the work commit through `ge.py commit hello H1 -m "..." hello.py test_hello.py`, `ge.py close hello H1 <hash> "pytest=1; greet exists"`, `ge/hello/kickoffs/H2.md` (H2 is what `ge.py unlocked hello H1` prints), then `ge.py commit hello H1 --close -m "ge(hello): close H1; unlocked H2"`. H2's kickoff to write: add `shout(name)` returning `greet(name).upper() + "!"` and `test_shout` asserting `shout("graph") == "HELLO, GRAPH!"`; gate `pytest`.
